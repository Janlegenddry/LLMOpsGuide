var e=[`模型`,`逐个`,`生成`,` token`,`。`];function t(e){let t=e.match(/[A-Za-z0-9]{1,4}|[^A-Za-z0-9]/gu)||[],n=new Map;return[{text:`<bos>`,id:1},{text:`<user>`,id:2},...t.map(e=>(n.has(e)||n.set(e,n.size+1e3),{text:e,id:n.get(e)})),{text:`<end>`,id:3},{text:`<assistant>`,id:4}]}function n(n){let r=t(n).length,i=[],a=[0,0,0,0],o=0,s=0,c=(e,t,n,r,c,l,u,d,f=-1)=>{i.push({key:e,title:t,node:n,text:r,input:c,output:l,watch:u,flow:d,round:f,phase:f<0?e===`done`?`结束`:`请求`:f===0?`Prefill`:`Decode`,generated:o,delivered:s,cache:[...a]})};c(`request`,`把对话封装成请求`,`client`,`客户端将用户消息、模型名、输出上限和 stream 参数编码为 JSON，经 HTTPS 送入服务。模型权重已在服务启动时加载到设备；每条请求不会重新加载一次权重。`,n,`POST /v1/chat/completions
{ messages: […], stream: true, max_tokens: 32 }`,`客户端记录请求开始时间，生成关联 ID；prompt 只用于本地演示，不发送到服务。`,`用户 → HTTPS / JSON → 网关`),c(`gateway`,`校验请求，选择模型副本`,`gateway`,`网关检查认证、配额和请求结构，再按模型版本、健康状态和路由策略选择一个推理副本。多副本是多个服务实例，不等同于单个模型的多 GPU 张量并行。`,`JSON / model = demo-decoder`,`rid = demo-request
route → replica B（示意）`,`网关耗时、拒绝率、路由目标；用 trace_id 关联下游，而不是用 prompt 做指标标签。`,`网关 → 服务发现 → replica B`),c(`tokenize`,`套用对话模板，再分词`,`tokenizer`,`角色和轮次边界先序列化为模型支持的 chat template，Tokenizer 再将文本编码为离散整数 ID。这里用简化切分和临时词表，ID 不是任何真实模型的 token ID；真实分词不保证一个汉字或单词对应一个 token。`,`<user>\n${n}\n<end><assistant>`,`input_ids: int64 [1, ${r}]\nposition_ids: 0 … ${r-1}`,`记录模板后输入 token 数和分词耗时，区分原始字符数与 token 数。`,`UTF-8 文本 → 模板 → 整数 ID`),c(`queue`,`进入等待队列`,`scheduler`,`请求进入目标副本的等待队列。调度器根据可用 KV 页、token budget 和当前运行请求决定何时接纳它；即使 GPU 正忙，队列中的请求也还没有执行本次前向计算。`,`waiting: A, 本请求, C\n本请求输入长度 S = ${r}`,`本请求：Waiting
其他请求：Decode 中`,`排队时间、waiting/running 请求数。排队变长可以让 TTFT 变差，而模型计算本身未必变慢。`,`副本 B → Waiting 队列`),c(`schedule`,`分配 KV 空间，组成执行批次`,`scheduler`,`调度器为请求分配可用 KV Cache 块并打包 token。连续批处理按迭代接纳新请求、移除已完成请求；长 prompt 可以分块 Prefill。本动画展示无前缀命中的单次完整 Prefill，聚焦 batch 中的一条请求。`,`本请求：${r} 个输入 token\n邻居请求：各 1 个 Decode token`,`Waiting → Running
打包 token / 块表 → model runner`,`本轮 scheduled tokens、KV 可用块、抢占次数；逻辑 batch 的请求数与 token 数是不同口径。`,`Scheduler → model runner → GPU`);for(let t=0;t<=e.length;t++){let n=t===0,i=n?r:1,l=r+t,u=e[t]??`<eos>`,d=n?`所有 prompt ID`:`上轮 token「${e[t-1]}」`,f=(e,n,r,i,a,o,s,l)=>c(e,n,r,i,a,o,s,l,t);n||f(`decode`,`Decode ${t}：把上轮 token 喂回模型`,`scheduler`,`上一个 token 已被选出并发往客户端；现在把它的 ID 作为本轮唯一的新输入。调度器可以重新组合 batch。权重保持不变，各层旧 K/V 继续保留，先前 token 的隐藏状态无需全部重算。`,`${d}\nposition = ${l-1}`,`input_ids: [1, 1]
本轮将预测下一个 token`,`每轮调度等待、Decode 耗时和 inter-token latency；本动画顺序展示，实际网络发送可与后续计算重叠。`,`上轮 token ID ↺ Scheduler → GPU`),f(`embed`,`Embedding：整数查表变成向量`,`embed`,`用 token ID 索引训练好的嵌入矩阵。文本的离散符号变成连续的隐藏向量，随后在每一层更新。玩具配置 d_model = 8、2 个注意力头、4 层，仅用于看清张量维度；不运行真实模型。`,`${d}\nIDs [1, ${i}]`,`X = E[IDs]\n[1, ${i}, 8] 浮点向量`,`H2D 拷贝、embedding kernel；ID 是整数，隐藏向量通常使用 BF16/FP16 等计算精度。`,`ID [1, ${i}] → X [1, ${i}, 8]`),f(`norm`,`第 1 层：RMSNorm 稳定数值尺度`,`norm`,`每个位置的向量沿隐藏维度归一化，并乘以学习到的缩放系数。这里采用 LLaMA 风格的 pre-norm 结构；并非所有 Transformer 都采用同一种归一化或层顺序。`,`X [1, ${i}, 8]`,`X̂ = X / √(mean(X²) + ε) ⊙ γ
张量形状不变`,`归一化只改数值，不改 token 数；实际执行可能与邻近算子融合。`,`隐藏向量 → 归一化隐藏向量`),f(`qkv`,`Q / K / V：同一输入，三组投影`,`qkv`,`X̂ 分别乘以 Wq、Wk、Wv。Q 表示当前位置要检索什么，K 用于匹配，V 提供聚合的信息。演示采用普通多头注意力 MHA，每头 d_head = 4；GQA 的 KV 头数会更少。`,`X̂ [1, ${i}, 8]`,`Q, K, V = X̂ Wq, X̂ Wk, X̂ Wv\n各为 [1, 2, ${i}, 4]（分头后）`,`QKV GEMM 耗时和设备带宽；这是线性代数运算，不是三个存放自然语言问题的数据库。`,`X̂ → Q ∥ K ∥ V`),a[0]=l,f(`rope`,`RoPE 与 KV 写入：加入位置信息`,`rope`,`按绝对位置旋转 Q 和 K 的成对维度，点积因此能反映相对位置；V 不做 RoPE。第 1 层将当前 token 的旋转后 K 和 V 写入本层缓存。其余层在各自执行时写入自己的 K/V。`,`position = ${n?`0 … ${l-1}`:l-1}\nQ / K / V`,`Q′, K′ = RoPE(Q, K)\n第 1 层 KV 长度 → ${l}`,`观察 KV 写入、分配失败和占用；这里显示逻辑长度，真实页式缓存还有块大小与碎片开销。`,`Q → RoPE；K → RoPE → Cache；V → Cache`),f(`attention`,`因果注意力：从允许的历史中聚合信息`,`attention`,n?`所有 prompt 位置可在一次前向中计算，但第 i 个位置只能看到自己和更早的 token。先求 QKᵀ / √d_head，将未来位置设为 −∞，逐行 softmax，再与 V 相乘。并行处理输入不意味着能看到未来。`:`本轮只有一个新的 Q，它读取本层所有历史 K 和当前 K，对历史 V 与当前 V 加权求和。缓存避免重算旧 K/V，但长上下文仍需读取更大的缓存；Decode 不是只看上一个 token。`,`Q [1, 2, ${i}, 4]\nK/V [1, 2, ${l}, 4]`,`A = softmax(QKᵀ / √4 + causal_mask)\nA [1, 2, ${i}, ${l}] → AV [1, 2, ${i}, 4]`,`关注 attention kernel 时长与 KV 读带宽。图里的注意力热图是因果可见性示意，不是模型解释或真实注意力权重。`,`Q × Kᵀ → 因果遮罩 → softmax → × V`),f(`residual`,`合并注意力头，接回残差路径`,`residual`,`将各头输出拼接，经 Wo 输出投影回 d_model，再加上进入注意力子层前的 X。残差提供信息和梯度的直接路径；推理只执行前向，不计算梯度或更新权重。`,`Concat(heads) [1, ${i}, 8]`,`H = X + Concat(heads) Wo
保留原信息并加入上下文信息`,`输出投影 GEMM；若采用张量并行，部分子层还会发生集合通信，这里未展开多 GPU。`,`Attention → Wo → + X`),f(`ffn`,`前馈网络：每个位置独立做非线性变换`,`ffn`,`H 先做 RMSNorm，再经过门控前馈网络 SwiGLU：一条支路经 SiLU 激活，与另一条支路逐元素相乘，最后投影回隐藏维度。该模块不跨 token 做注意力，而是转换每个位置已汇聚的信息。`,`U = RMSNorm(H)\n[1, ${i}, 8]`,`MLP(U) = [SiLU(U Wg) ⊙ (U Wu)] Wd
8 → 24 → 8（演示维度）`,`FFN GEMM 与激活耗时。本页为稠密模型；MoE 会在此加入专家路由与可能的跨卡通信。`,`Norm → Gate ∥ Up → ⊙ → Down`),a=[l,l,l,l],f(`layers`,`残差相加，再依次通过剩余层`,`layers`,`先得到第 1 层输出 H + MLP(U)，随后第 2～4 层各自重复 Norm、Attention、残差、FFN、残差。每层有独立权重和独立 KV。动画合并展示后三层，不代表模型跳过它们，也不代表它们在同一条请求上同时执行。`,`第 1 层输出 [1, T, 8]`,`第 4 层输出 [1, ${i}, 8]\n4 层 KV 均包含 ${l} 个位置`,`用 GPU profiler 分解 kernel 和通信，服务级 Trace 不应默认为每个 token 的每一层建立高成本 span。`,`第 1 层 → 第 2 层 → 第 3 层 → 第 4 层`),f(`head`,`LM Head：最后位置变成词表分数`,`head`,`最终 RMSNorm 后，把最后一个输入位置的隐藏向量投影到词表维度，得到每个候选 token 的 logit。Prefill 的最后位置就可预测第一个输出 token；不需要先额外跑一轮 Decode。`,`最后位置 h [1, 8]
不是整段答案`,`logits = FinalNorm(h) W_vocab
[1, Vocab]（展示 4 个玩具候选）`,`logit 是未归一化分数。真实词表通常更大，图中分数由教学脚本指定，不来自 prompt 的语义计算。`,`最后位置向量 → 全词表 logits`),t<e.length&&o++,f(`sample`,u===`<eos>`?`选择 EOS：满足停止条件`:`选择 token「${u}」`,`sample`,u===`<eos>`?`这一轮 EOS 得分最高，结束生成，不把 EOS 作为普通文本发给用户。真实服务还会检查 max_tokens、停止串、上下文上限及客户端取消；不是所有结束都由 EOS 触发。`:`演示使用贪心策略，选择 logit 最大的 ID；softmax 条形图帮助理解相对概率，贪心本身不必先计算 softmax。随机采样可使用 temperature、top-k 或 top-p；模型做的是条件预测，没有检索一个预先存好的完整答案。`,`示意 logits [3.2, 1.6, 0.8, −0.4]`,`${u===`<eos>`?`eos_id = 5`:`next_token_id = ${100+t}`}\n选中：${u}`,`采样耗时、生成 token 数、停止原因。为了稳定讲解，本页的回答由脚本预设，不随自定义 prompt 生成真实答案。`,`logits → argmax → 一个 token ID`),t<e.length&&(s++,f(`stream`,t===0?`首个 token 返回：用户开始看到文字`:`第 ${t+1} 个片段返回客户端`,`client`,`Detokenizer 把 token ID 增量解码成文本，服务将可发送的文本放进流式事件，网关转发，客户端持续追加显示。真实 token 可能只是字节或子词，需要缓冲后才能显示；一个网络 chunk 不一定对应一个 token。`,`token_id = ${100+t}`,`data: ${JSON.stringify({delta:{content:u}})}\n\n`,t===0?`客户端从请求开始到首个可用输出的时间是 TTFT；服务端首 token 与客户端首字的测量边界可能不同。`:`客户端相邻可用输出的间隔是 ITL；网络缓冲会改变观测到的节奏，不能把动画播放时间当作推理性能。`,`token ID → Detokenizer → SSE → 网关 → 用户`))}return a=[0,0,0,0],c(`done`,`关闭响应，释放请求资源`,`client`,`服务发送结束事件和使用量信息，关闭流，将该请求从 running 集合移除并释放引用的 KV 块。启用前缀缓存时，部分块可能作为可复用前缀保留；本演示关闭前缀缓存并显示完全释放。`,`finish_reason = "stop"
EOS 已选中`,`data: [DONE]\n可见文本：${e.join(``)}`,`记录总时延、输入/输出 token 数和结束原因，结束请求 span。不同服务对特殊 token 的 usage 计数可能不同。`,`完成信号 → 用户；KV 引用 → 回收`),i}function r(e=[3.2,1.6,.8,-.4]){let t=Math.max(...e),n=e.map(e=>Math.exp(e-t)),r=n.reduce((e,t)=>e+t,0);return n.map(e=>e/r)}function i(){let i=document.querySelector(`#inference-lab`);if(!i||i.dataset.ready)return;i.dataset.ready=`true`;let a=e=>i.querySelector(`#lab-${e}`),o=a(`prompt`),s=a(`play`),c=a(`progress`),l=a(`speed`),u=o.value,d=n(u),f=0,p=!1,m,h=matchMedia(`(prefers-reduced-motion: reduce)`),g=matchMedia(`(min-width: 40rem)`),_=i.querySelector(`.lab-map`);_.open=g.matches,g.addEventListener(`change`,()=>{_.open=g.matches});let v=(e,t)=>{a(e).textContent=t};function y(e,t,n=``){let r=document.createElement(e);return r.textContent=t,r.className=n,r}function b(e,t){let n=y(`div`,``,`lab-pills`);t.forEach((e,t)=>{let r=y(`span`,e);r.style.setProperty(`--item-delay`,`${t*140}ms`),n.append(r)}),e.append(n)}function x(n){let i=a(`visual-content`);i.replaceChildren();let o=t(u),s=o.length;if(v(`visual-label`,`教学示意 · 非真实模型数值`),[`head`,`sample`].includes(n.key)){v(`visual-title`,`候选 token 的概率`);let t=[e[n.round]??`<eos>`,`一个`,`继续`,`其他`];r().forEach((e,n)=>{let r=y(`div`,``,`lab-prob-row`);r.append(y(`span`,t[n]),y(`code`,`${(e*100).toFixed(1)}%`));let a=y(`div`,``,`lab-prob-track`),o=y(`span`,``);o.style.transform=`scaleX(${e})`,a.append(o),r.append(a),i.append(r)}),i.append(y(`p`,`4 项玩具词表：softmax([3.2, 1.6, 0.8, −0.4])。贪心选择最大项；概率不是置信度保证。`,`lab-small`))}else if(n.key===`attention`){v(`visual-title`,n.round===0?`因果遮罩 · 左下三角可见`:`新 Q 读取所有历史 K/V`);let e=n.round===0?Math.min(s,6):1,t=Math.min(s+n.round,6),r=y(`div`,``,`lab-attention-matrix`);r.style.gridTemplateColumns=`repeat(${t}, minmax(0, 1fr))`;for(let i=0;i<e;i++)for(let e=0;e<t;e++){let t=n.round===0&&e>i,a=y(`span`,t?`−∞`:`可见`,t?`masked`:`allowed`);a.style.setProperty(`--item-delay`,`${i*120+e*30}ms`),r.append(a)}i.append(r,y(`p`,n.round===0?`截取前 ${t} 个位置。每行是一个 Q，每列是一个 K；未来位置在 softmax 后权重为 0。`:`仅展示前 ${t} 个历史位置；这个新 Q 实际可读取 ${s+n.round} 个 K/V 位置（含自身）。`,`lab-small`))}else if([`embed`,`norm`,`qkv`,`rope`,`residual`,`ffn`,`layers`].includes(n.key)){v(`visual-title`,`张量如何变化`);let e=n.round===0?s:1;b(i,{embed:[`IDs [1, ${e}]`,`E [Vocab, 8]`,`X [1, ${e}, 8]`],norm:[`X [1, ${e}, 8]`,`沿 d=8 归一化`,`X̂ [1, ${e}, 8]`],qkv:[`Wq / Wk / Wv [8, 8]`,`Q [1, 2, ${e}, 4]`,`K [1, 2, ${e}, 4]`,`V [1, 2, ${e}, 4]`],rope:[`Q → 旋转 Q′`,`K → 旋转 K′ → 缓存`,`V → 缓存（不旋转）`],residual:[`2 个头 × ${e} 位置 × 4 维`,`拼接 → [1, ${e}, 8]`,`乘 Wo，再 + X`],ffn:[`[1, ${e}, 8]`,`Gate / Up [1, ${e}, 24]`,`SiLU(Gate) ⊙ Up`,`Down [1, ${e}, 8]`],layers:[`第 1 层 + FFN 残差`,`→ 第 2 层`,`→ 第 3 层`,`→ 第 4 层`]}[n.key]);let t=y(`div`,``,`lab-vector-matrix`);for(let e=0;e<24;e++){let n=y(`span`,``);n.style.setProperty(`--cell-opacity`,String(.18+(e*7+f*3)%11/14)),n.style.setProperty(`--item-delay`,`${e*22}ms`),t.append(n)}i.append(t,y(`p`,`色块表示浮点张量的局部切片；颜色无数值含义。形状中的顺序为 batch、序列、隐藏维，分头后为 batch、head、序列、head_dim。`,`lab-small`))}else if([`queue`,`schedule`,`decode`].includes(n.key))v(`visual-title`,`一个连续批处理迭代`),(n.key===`queue`?[`A · 正在 Decode`,`本请求 · Waiting`,`C · Waiting`]:[`A · Decode 1 token`,`本请求 · ${n.round>0?`Decode 1`:`Prefill ${s}`} token`,`C · 继续等待`]).forEach((e,t)=>i.append(y(`div`,e,`lab-queue-item ${t===1?`is-selected`:``}`))),i.append(y(`p`,`调度策略因引擎、版本和配置而异。邻居请求仅用于说明共享 token budget，不参与本页的张量计数。`,`lab-small`));else if(n.key===`stream`||n.key===`done`)v(`visual-title`,`从 ID 到可见文本`),b(i,n.key===`done`?[`EOS`,`finish_reason: stop`,`[DONE]`,`KV 引用归零`]:[`ID ${100+n.round}`,`文本「${e[n.round]}」`,`SSE data 事件`,`浏览器追加显示`]),i.append(y(`p`,`这里为每个演示 token 发送一个文本片段。真实服务可能合并事件、缓冲不完整字符，或使用其他流式协议。`,`lab-small`));else if(v(`visual-title`,n.key===`gateway`?`路由到健康副本`:`文本 → 教学 token / ID`),n.key===`gateway`)b(i,[`replica A · 高负载`,`replica B · 已选择`,`replica C · 不健康`]);else if(n.key===`request`)b(i,[`用户输入`,`messages: [{ role: user, content: … }]`,`HTTPS 请求`]),i.append(y(`p`,`此刻还是文本与请求参数；到 Tokenizer 节点才会变为整数 ID。`,`lab-small`));else{let e=y(`div`,``,`lab-token-list`);o.forEach(t=>{let n=y(`span`,``,`lab-token`);n.append(y(`b`,t.text.replace(/ /g,`␠`).replace(/\n/g,`↵`)),y(`small`,String(t.id))),e.append(n)}),i.append(e,y(`p`,`S = ${s} 个教学 token，包含 4 个模板边界 token。重复文本片段复用同一 ID；这不是模型词表。`,`lab-small`))}}function S(){let t=d[f];i.dataset.playing=String(p),i.dataset.step=t.key,v(`position`,`${f+1} / ${d.length}`),c.max=String(d.length-1),c.value=String(f),c.setAttribute(`aria-valuetext`,`第 ${f+1} 步，${t.title}`),s.textContent=p?`暂停动画`:f===d.length-1?`重新播放`:`播放动画`,s.setAttribute(`aria-pressed`,String(p)),a(`prev`).disabled=f===0,a(`next`).disabled=f===d.length-1,v(`phase`,t.phase),v(`round`,t.round<0?`请求生命周期`:t.round===0?`处理整个 prompt → 预测第 1 个 token`:`Decode 第 ${t.round} 轮 → ${t.round===e.length?`预测 EOS`:`预测第 ${t.round+1} 个 token`}`),v(`step-label`,`STEP ${String(f+1).padStart(2,`0`)} / ${t.phase.toUpperCase()}`),v(`step-title`,t.title),v(`step-text`,t.text),v(`data-in`,t.input),v(`data-out`,t.output),v(`flow`,t.flow),v(`watch-text`,t.watch),v(`play-status`,p?`正在播放`:f===d.length-1?`演示完成`:`已暂停，可单步查看`),i.querySelectorAll(`[data-node]`).forEach(e=>{let n=e.dataset.node===t.node;e.classList.toggle(`is-active`,n),n?e.setAttribute(`aria-current`,`step`):e.removeAttribute(`aria-current`)}),i.querySelectorAll(`[data-chapter]`).forEach(e=>e.setAttribute(`aria-pressed`,String(e.dataset.chapter===t.phase)));let n=a(`cache-rows`);n.replaceChildren(),t.cache.forEach((e,r)=>{let i=y(`div`,``,`lab-cache-row`);i.append(y(`span`,`L${r+1}`));let a=y(`div`,``,`lab-cache-cells`);for(let n=0;n<Math.min(e,18);n++){let i=y(`i`,``);(t.key===`rope`&&r===0||t.key===`layers`&&r>0)&&(t.round===0||n===Math.min(e,18)-1)&&(i.className=`new-kv`),a.append(i)}e>18&&a.append(y(`small`,`+${e-18}`)),i.append(a,y(`code`,`${e} 位置`)),n.append(i)});let r=t.cache.reduce((e,t)=>e+t,0);v(`cache-summary`,`${r*2*2*4} 个浮点元素`),v(`output`,t.delivered?e.slice(0,t.delivered).join(``):`等待首个 token…`),v(`output-count`,`${t.delivered} 个已返回 / ${t.generated} 个已选中（不含 EOS）`),v(`stream-events`,t.key===`done`?`finish_reason: stop · data: [DONE]`:t.delivered?`最后片段：${JSON.stringify(e[t.delivered-1])}`:`连接建立后保持等待，直到首个可用文本片段`);let o=a(`event-list`);o.replaceChildren(),d.slice(Math.max(0,f-3),f+1).forEach((e,t,n)=>{let r=f-n.length+t+2;o.append(y(`li`,`${String(r).padStart(2,`0`)}  ${e.title}`))}),x(t),h.matches||(a(`flow`).getAnimations().forEach(e=>e.cancel()),a(`flow`).animate([{opacity:.3,transform:`translateX(-8px)`},{opacity:1,transform:`translateX(0)`}],{duration:400}))}function C(){clearTimeout(m),p=!1}function w(){clearTimeout(m),p&&(m=setTimeout(()=>{f<d.length-1&&f++,f===d.length-1&&C(),S(),w()},4500/Number(l.value)))}function T(e){C(),f=Math.max(0,Math.min(e,d.length-1)),S()}s.addEventListener(`click`,()=>{p?C():(f===d.length-1&&(f=0),p=!0),S(),w()}),a(`prev`).addEventListener(`click`,()=>T(f-1)),a(`next`).addEventListener(`click`,()=>T(f+1)),a(`reset`).addEventListener(`click`,()=>T(0)),c.addEventListener(`input`,()=>T(Number(c.value))),l.addEventListener(`change`,w),a(`form`).addEventListener(`submit`,e=>{if(e.preventDefault(),!o.value.trim()){o.setCustomValidity(`请输入非空 prompt。`),o.reportValidity();return}u=o.value,d=n(u),T(0),v(`play-status`,`新请求已载入，点击播放或下一步`)}),o.addEventListener(`input`,()=>{o.setCustomValidity(``),C(),S(),v(`play-status`,`输入已修改，点击“载入请求”后生效`)}),a(`example`).addEventListener(`click`,()=>{o.value=o.value===`Why is the sky blue?`?`用一句话解释 KV Cache。`:`Why is the sky blue?`,o.setCustomValidity(``),u=o.value,d=n(u),T(0)}),i.querySelectorAll(`[data-chapter]`).forEach(e=>e.addEventListener(`click`,()=>T(d.findIndex(t=>t.phase===e.dataset.chapter)))),i.querySelectorAll(`[data-node]`).forEach(e=>e.addEventListener(`click`,()=>{let t=Math.max(0,d[f].round),n=d.findIndex(n=>n.node===e.dataset.node&&n.round===t);n<0&&(n=d.findIndex(t=>t.node===e.dataset.node)),n>=0&&(T(n),g.matches||(_.open=!1))})),document.addEventListener(`visibilitychange`,()=>{document.hidden&&(C(),S())}),window.addEventListener(`pagehide`,C),S()}i();